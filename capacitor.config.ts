import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'udemy-ionic5-qrscanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
